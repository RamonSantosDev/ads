#include <stdio.h>

int main () {

   /* int a = -10, b = 5;

    if (a > 0 || b > 0) // if (a > 0 && b > 0)
    {
        printf("Os dois numeros sao positivo.\n");
    }else
    {
       printf("pelo menos um dois numeros é negativo\n");
    }
   */

   int a = 4;

   if(!(a > 5)) {
    printf("A variavel é negativa\n");
   }else{
    printf("A variavel é positiva");
   }

}