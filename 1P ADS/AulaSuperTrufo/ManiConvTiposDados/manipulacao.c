#include <stdio.h>

int main (){
    /*
    // variaveis inteiras
    int a = 10;
    int b = 3;
    int soma = a + b;
    int diferenca = a - b;
    int produto = a * b;
    int quociente = a / b; // note que a divisão de inteiros resulta em um numero inteiro ele quebra

    printf("Soma: %d\n", soma);
    printf("Diferença: %d\n", diferenca);
    printf("Produto: %d\n", produto);
    printf("Quociente: %d\n", quociente);

    */
   
    /*
    // variaveis de ponto flutuantes
    float x = 5.5;
    float y = 2.2;
    float soma = x + y;
    float diferenca = x - y;
    float produto = x * y;
    float quociente = x / y;

    printf("Soma: %.2f\n", soma);
    printf("Diferença: %.2f\n", diferenca);
    printf("Produto: %.2f\n", produto);
    printf("Quociente: %.2f\n", quociente);
    */
    /*
    //conversao de tipos de dados   
    // conversao implicita
    int a = 10;
    float b = 3.5;
    float resultado = a + b; // "a" foi convertido implicitamente para o tipo float

    printf("resultado %.2f\n", resultado);
    */
    /*
    // conversao explicita
    int a = 10;
    int b = 3;        //cache
    float quociente = (float) a / b; // 'a' é explicitamente convertido para float
    printf("Quociente: %.2f\n", quociente);
    */
    return 0;

}